name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: shellcheck
        run: shellcheck -S warning -e SC1090,SC1091 wcdiag lib/*.sh tests/run.sh

      - name: offline tests
        run: bash tests/run.sh
